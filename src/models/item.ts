// Define um tipo chamado Item
type Item = {
    id?: number; // Propriedade opcional
    nome: string; // Propriedade obrigatória
};

// Exporta o tipo Item para que ele possa ser utilizado em outros arquivos do projeto
export default Item;